
def what_language(ext):
    """
    if(ext=='?'):
        return('c')
    """

    if(ext=='sh'):
        return('shellscript')

    """
    if(ext=='?'):
        return('coffeescript')
    
    if(ext=='?'):
        return('cpp')

    if(ext=='?'):
        return('css')
    
    if(ext=='?'):
        return('dockerfile')

    if(ext=='?'):
        return('fsharp')

    if(ext=='?'):
        return('go')
    
    if(ext=='?'):
        return('groovy')

    if(ext=='?'):
        return('haskell')

    if(ext=='?'):
        return('ini')
    
    if(ext=='?'):
        return('jade')
    
    if(ext=='?'):
        return('java')
    
    if(ext=='?'):
        return('javascript')
    
    if(ext=='?'):
        return('javascriptreact')
    
    if(ext=='?'):
        return('latex')
    
    if(ext=='?'):
        return('less')
    
    if(ext=='?'):
        return('lua')
    
    if(ext=='?'):
        return('makefile')
    
    if(ext=='?'):
        return('objective-c')
    
    if(ext=='?'):
        return('ocaml')
    
    if(ext=='?'):
        return('perl')
    
    if(ext=='?'):
        return('perl6')
    
    if(ext=='?'):
        return('php')
    
    if(ext=='?'):
        return('plaintext')
    
    if(ext=='?'):
        return('powershell')
    """

    if(ext=='py'):
        return('python')

    """"
    if(ext=='?'):
        return('r')
    
    if(ext=='?'):
        return('ruby')
    
    if(ext=='?'):
        return('rust')
    
    if(ext=='?'):
        return('scss')
    
    if(ext=='?'):
        return('sql')
    
    if(ext=='?'):
        return('swift')
    
    if(ext=='?'):
        return('typescript')
    
    if(ext=='?'):
        return('typescriptreact')

    if(ext=='?'):
        return('xsl')
    
    if(ext=='?'):
        return('yaml')

    """
    pass